{
  "name": "vite-plugin-apitemkin-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/vite-plugin-apitemkin",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "dev:playground": "npm run dev -w packages/playground"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsdown": "^0.10.0",
    "typescript": "^5.5.0",
    "vite": "^7.0.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
